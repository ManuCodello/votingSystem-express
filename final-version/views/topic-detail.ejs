<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= topic.title %> - Learn It, Love It</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <a href="/" class="back-link">← Volver a la lista</a>
    <h1 class="main-title"><%= topic.title %></h1>

    <div class="form-container">
      <form id="add-subtopic-form">
        <input type="text" name="title" placeholder="Título del subtema" class="topic-form__input" required>
        <input type="url" name="link" placeholder="https://enlace.util" class="topic-form__input" required>
        <button type="submit" class="topic-form__button">Añadir Subtema</button>
      </form>
    </div>

    <h2>Subtemas y Enlaces</h2>
        <div class="subtopic-list">
        
        <% if (subtopics && subtopics.length > 0) { %>
            <% subtopics.forEach(subtopic => { %>
            <div class="subtopic" data-id="<%= subtopic.id %>">
                <a href="<%= subtopic.link %>" target="_blank" class="subtopic__title"><%= subtopic.title %></a>
                <div class="subtopic__actions">
                <button class="subtopic__vote-btn js-upvote-btn" data-id="<%= subtopic.id %>">▲</button>
                <span class="subtopic__votes"><%= subtopic.votes %></span>
                <button class="subtopic__vote-btn js-downvote-btn" data-id="<%= subtopic.id %>">▼</button>
                </div>
            </div>
            <% }) %>
        <% } else { %>
            <p class="empty-list-message">Aún no hay subtemas. ¡Sé el primero en añadir uno!</p>
        <% } %>
        
        </div>
  </div>

    <script>
    // Pasamos el ID del tema principal al script de cliente
        const TOPIC_ID = <%- JSON.stringify(topic.id) %>;
    </script>
    <script src="/js/topic-detail.js"></script>
</body>
</html>